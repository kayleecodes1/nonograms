import styled from 'styled-components';
import { PuzzleContainerTemplateArea } from '../PuzzleContainer.styles';

export const Root = styled.div({
    gridArea: PuzzleContainerTemplateArea.Main,
    display: 'flex',
});
